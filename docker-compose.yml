version: '3.8'

services:
  backend:
    build: .
    container_name: backend
    expose:
      - 3000
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm start
    restart: always

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: always